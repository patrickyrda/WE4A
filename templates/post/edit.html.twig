{% extends 'base.html.twig' %}

{% block title %}Modifier un Post{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Modifier le Post</h1>

    {{ include('post/_form.html.twig', { form: form }) }}

    <a href="{{ path('app_post_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
</div>
{% endblock %}
